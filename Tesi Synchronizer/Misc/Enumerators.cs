namespace Synchronizer
{
    public static class Enumerators
    {
        public enum TipoUsuaio { Admin, Fleury };

        public enum CheckStatus { Check, Uncheck };

        public enum Grupo { Todos, Fleury, Amais, Hosp, Sp, Rj, Ba, Pe };
    }
}
